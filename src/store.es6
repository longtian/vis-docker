import {DataSet} from "vis";

const events = new DataSet();
const nodes = new DataSet();
const edges = new DataSet();

// @todo decide whether to move dataview and seach logic here

export {
  events,
  nodes,
  edges
}